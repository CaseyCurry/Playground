const expect = require("chai")
  .expect;

describe("unit test suite", () => {
  it("should run a test", () => {
    expect(true)
      .to
      .equal(true);
  });

  it("should run another test", () => {
    expect(true)
      .to
      .equal(true);
  })
});
